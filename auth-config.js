/* Public browser configuration only. Leave blank for the visual demo.
 * Fill with your Supabase PROJECT URL and publishable/anon key after deployment.
 * NEVER put service_role keys, provider client secrets, Gmail tokens, or API secrets here.
 */
window.ESTATEIQ_AUTH_CONFIG = {
  supabaseUrl: '',
  supabaseAnonKey: ''
};
