/* Public endpoint URL only; never place an OpenAI API key in website JavaScript.
   Deploy worker/ and enter its HTTPS URL. Blank = offline scripted demonstration. */
window.ESTATEIQ_LIVE_AI = { endpoint: '' };
